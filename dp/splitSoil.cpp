#include <iostream>
#include <string.h>

using namespace std;

int n, m, a[100][100], ans, x[5], y[5];

bool jc(int k, int mid) 
{
    if (k == 5)
        y[k] = y[k-1];
}

int main()
{
    cin >> n >> m ;
    for (int i = 1; i )
}
